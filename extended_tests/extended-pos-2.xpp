/* test case for image cropping */
func int main() {
    arr img1 = load("./darli-fra.png");
    arr img2 = crop(img1,  0, 700, 1080, 600);
    save(img2, "./darli-fra_cropped.png");
    close(img1, 0);
    return 0;
}
